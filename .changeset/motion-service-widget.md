---
'@viamrobotics/test-widgets': minor
---

Add a motion service test widget with a Move section, exported as `MotionServiceWidget` alongside the standalone `MotionMoveWidget` and registered in the resource-widget lookup helpers. The motion service stays hidden from the default control view for now.
