---
'@viamrobotics/test-widgets': minor
---

Add optional controlled `input` and `onInputChange` props to `DoCommandWidget` so callers can set the editor value without going through the header snippet
