---
'@viamrobotics/test-widgets': patch
---

Dedupe errors in `Queries` so widgets passing multiple queries against one client no longer crash with `each_key_duplicate` when those queries fail identically
