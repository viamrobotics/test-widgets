---
'@viamrobotics/test-widgets': minor
---

The camera widget accepts an `autoplay` prop. A host passes it when the user expands a collapsed card so the feed starts even though "Start feed automatically" is off; the saved preference itself is left as is.
