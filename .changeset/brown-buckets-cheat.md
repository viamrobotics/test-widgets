---
'@viamrobotics/test-widgets': patch
---

Arm, encoder, and power-sensor widgets collapse to one column in narrow cards via container queries; input-controller, ML model, and camera header rows wrap; operations and sessions tables scroll horizontally inside their box instead of truncating
